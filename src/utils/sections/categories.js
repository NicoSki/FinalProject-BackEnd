// //*I´ve created this util becuase i wanted to separated the categories and show them in the 
// //* differents cards at the main page so i will use the schema of mongoose:

// let ProductSchema = require("../../models/productModel");

// let filter;

// (async () => {
//     try {
//         filter = await ProductSchema.find({});
//         return filter
//     } catch (error) {
//         //*CAMBIAR EL LOG
//         console.log(error);
//     }
// })();

// module.exports = { filter };